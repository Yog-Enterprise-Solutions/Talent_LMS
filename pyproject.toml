[project]
name = "talent_lms"
version = "0.0.1"
authors = [
    { name = "Jay", email = "sarnajay@gmail.com"}
]
description = "talent lms"
requires-python = ">=3.10"
readme = "README.md"

dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.dev-dependencies]

[tool.bench.frappe-dependencies]
frappe = "~=15.0"